[package]
name = "rust-file-compression"
version = "0.1.0"
edition = "2024"

[dependencies]
flate2 = { version = "1.0.20", features = ["zlib-ng"], default-features = false}
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres"] }
tokio = { version = "1", features = ["full"] }