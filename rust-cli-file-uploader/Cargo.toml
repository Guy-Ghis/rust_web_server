[package]
name = "rust-cli-file-uploader"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = {version = "0.11", features = ["blocking", "json"]}
clap = { version = "4.0"}
tokio = {version = "1", features = ["full"]}
